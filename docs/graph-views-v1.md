# Graph views v1

Three readings of objects that already exist.

| View | Object | Does | Does not |
| --- | --- | --- | --- |
| Spectral | atlas adjacency | Laplacian spectrum as connectivity invariant | cluster rooms or replace Guid |
| Functional | ledger events | out-degree-1 world map | mixture / branching belief |
| Factor | Office-A slots + P-204 | named local functions | sum-product, sparse Sigma |

Dense Beam-B1 remains the estimator. A factor list is not inference.
JSPT still owns `J Sigma J^T`. RCI still owns the record. Torus lengths
stay algebraic and stay out of a guest.

```bash
python -m gat.demo.graph_views -o out/graph-views
```
