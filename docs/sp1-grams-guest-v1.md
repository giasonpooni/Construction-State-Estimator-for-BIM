# SP1 grams guest v1

The discrete TPT^T kernel and `grams-micro-v1` receipt live in
Jacobian-Sensitivity-Propagation-Testbed (`sensitivity.grams_guest`,
`rust/grams-guest`).

GAT may wrap that receipt in an SP1 program using the same pin as the
beam guest. GAT may not implement another TPT^T.

Non-claims: not kappa2, not NIS, not inspection, not IEEE-754.
