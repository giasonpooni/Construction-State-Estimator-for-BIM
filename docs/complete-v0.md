# Complete v0

```bash
python -m gat.demo.complete_v0 -o out/complete-v0
```

`STATUS.json` is complete when the opening component is connected, Beam-B1
worlds are cited not merged, the Beam guest is admitted as checked-integer,
and SP1 cycles are still null.

It is not occupancy. Isolated slots stay isolated. Field evidence stays false.
