# Uncertainty budget cite

Canonical implementation: RCI `instrument_chain.uncertainty_budget`.
Schema `uncertainty-budget-v1`. CSE Observe may consume a declared `u_c`
from that record. It does not compute a GUM budget of its own and it
does not change `traceability` from `none_claimed`.
