# Harness fixtures

Shipped so `python -m gat.demo.experiment_harness --demo` and
`--inspectability --demo` work after clone.

- `rci-example-commitment-v1.json` is not field evidence.
- `torus-example-commitment-v1.json` is not a live validation pin.
- Neither file is Beam-B1 evidence.
- `space-l3-office-a.json` and `opening-17-receipt.json` are identity
  and receipt stand-ins for the inspectability fold. They are not a bind.
