# Office-A present packet (process start)

This snapshot is the live `--demo` process **before** bind, calibration,
and observation. Verification already passed. Stamp is refused.

Regenerate:

```bash
python -m gat.demo.present_process --demo -o validation/present-packet-office-a
```

`01-verify.json` is not pinned here: world digest includes the model path.
