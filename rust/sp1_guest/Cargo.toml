[package]
name = "cse-sp1-guest"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Guest-shaped host runner for the i32 kernels. Not an SP1 prove."
publish = false

[dependencies]
cse-fixedpoint-kernel = { path = "../fixedpoint_kernel" }
