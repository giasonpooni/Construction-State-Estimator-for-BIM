[package]
name = "cse-effort-gate"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Live satellite-effort authority. Policy only. Does not run CUDA or SP1."
publish = false

[dependencies]
serde_json = "1"
